name: Build and Push Docker Images
on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Get GitHub version
        id: version
        run: echo "::set-output name=tag::$(git rev-parse --short HEAD)"

      - name: Build and push app service image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: app-service:${{ steps.version.outputs.tag }}

      - name: Build and push db service image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./docker-compose.yml
          push: true
          tags: db-service:${{ steps.version.outputs.tag }}
          target: db

      - name: Build and push hasura service image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./docker-compose.yml
          push: true
          tags: hasura-service:${{ steps.version.outputs.tag }}
          target: hasura
